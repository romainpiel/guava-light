apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile project(':guava-base')
    compile project(':guava-primitives')
    compile project(':guava-math')

    testCompile 'junit:junit:4.12'
}